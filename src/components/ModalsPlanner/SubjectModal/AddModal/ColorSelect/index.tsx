import React, { useState } from 'react'
import { SelectColorWrapper, ColorPickerButton } from '../TimeSelect/styled'

//옵션 시간 추가 필요요
export const ColorSelect: React.FC = () => {
  return <SelectColorWrapper></SelectColorWrapper>
}
